import { Student } from '../models/student.model';

export class SchoolDistrict {
  id: number;
  alternateRate: number;
  rate: number;
  name: string;
  aun: number;
  paymentType: string;
  created: Date;
  lastUpdate: Date;
}
