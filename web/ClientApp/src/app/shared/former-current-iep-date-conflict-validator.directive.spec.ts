import { FormerCurrentIepDateConflictValidatorDirective } from './former-current-iep-date-conflict-validator.directive';

describe('FormerCurrentIepDateConflictValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormerCurrentIepDateConflictValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
