import { EnrollmentWithdrawalDateConflictValidatorDirective } from './enrollment-withdrawal-date-conflict-validator.directive';

describe('EnrollmentWithdrawalDateConflictValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EnrollmentWithdrawalDateConflictValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
