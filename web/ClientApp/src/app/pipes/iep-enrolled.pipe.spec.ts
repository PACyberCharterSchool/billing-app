import { IepEnrolledPipe } from './iep-enrolled.pipe';

describe('IepEnrolledPipe', () => {
  it('create an instance', () => {
    const pipe = new IepEnrolledPipe();
    expect(pipe).toBeTruthy();
  });
});
