import { NormalizeFieldValuePipe } from './normalize-field-value.pipe';

describe('NormalizeFieldValuePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizeFieldValuePipe();
    expect(pipe).toBeTruthy();
  });
});
