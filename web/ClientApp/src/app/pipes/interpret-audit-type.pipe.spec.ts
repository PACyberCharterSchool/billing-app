import { InterpretAuditTypePipe } from './interpret-audit-type.pipe';

describe('InterpretAuditTypePipe', () => {
  it('create an instance', () => {
    const pipe = new InterpretAuditTypePipe();
    expect(pipe).toBeTruthy();
  });
});
