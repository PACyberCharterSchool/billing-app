import { NormalizeFieldNamePipe } from './normalize-field-name.pipe';

describe('NormalizeFieldNamePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizeFieldNamePipe();
    expect(pipe).toBeTruthy();
  });
});
